// This file has been generated by TauRPC. Do not edit this file manually.
import {
	createTauRPCProxy as createProxy,
	type InferCommandOutput,
	type TauRpcResult,
} from "taurpc"

export type AccountProfile = {
	id: string
	accountType: "ely" | "microsoft" | "offline"
	username: string
	uuid: string
	skinUrl: string | null
	isActive: boolean
	createdAt: number
}

const ARGS_MAP = {
	"": {
		greet: ["name"],
		ely_login: ["username", "password"],
		add_offline_account: ["username"],
		get_accounts: [],
		set_active_account: ["account_id"],
		remove_account: ["account_id"],
		get_active_account_token: [],
	},
}

const RESULT_MAP = {
	"": {
		greet: false,
		ely_login: true,
		add_offline_account: true,
		get_accounts: true,
		set_active_account: true,
		remove_account: true,
		get_active_account_token: true,
	},
}

export type Router = {
	"": {
		greet: (name: string) => Promise<string>
		ely_login: (username: string, password: string) => Promise<AccountProfile>
		add_offline_account: (username: string) => Promise<AccountProfile>
		get_accounts: () => Promise<AccountProfile[]>
		set_active_account: (accountId: string) => Promise<void>
		remove_account: (accountId: string) => Promise<void>
		get_active_account_token: () => Promise<string>
	}
}

export const createTauRPCProxy = () =>
	createProxy<Router>({
		argsMap: ARGS_MAP,
		resultMap: RESULT_MAP,
		errorHandling: "throw",
	})

export type { InferCommandOutput, TauRpcResult }
